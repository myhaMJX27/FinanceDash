import type { TTransactionOperationMap } from '../../model/types';

export const CMostExpensiveLength = 5;

export const CTransactionOperation: TTransactionOperationMap = {
  income: 'income',
  expense: 'expense',
};
