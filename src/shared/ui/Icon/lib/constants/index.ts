import type { TIconStyleMap } from '../../model/types';
import {
  StyledIconActive,
  StyledIconDark,
  StyledIconLight,
} from '../../ui/Styledicons';

export const CIconStyle: TIconStyleMap = {
  dark: StyledIconDark,
  light: StyledIconLight,
  active: StyledIconActive,
};
